# 智能装配大赛协作规则

## 工作范围

- 当前主实现位于 `线下赛/决赛代码`；除非用户明确要求，不要把线上赛版本当作决赛主版本修改。
- `线上赛`中的源码是历史参考版本，避免无关重构。
- 修改前阅读 `context/PROJECT_STATE.md`、`context/DECISIONS.md` 和最近的 `context/TASK_LOG.md`。

## 实施原则

- 先明确成功标准、隐含前提和测试方式，再修改代码。
- 仅修改需求直接涉及的文件，保持现有结构与风格。
- 修复缺陷时补充或更新复现测试；完成后运行与风险相称的测试。
- 不猜测实机 IP、端口、坐标、IO 接线、相机标定或 VisionMaster 流程名；缺失时明确列为待实机确认项。

## 同步安全

- 不提交 `.codex`、`.env`、API 密钥、凭据、虚拟环境、缓存、机器专属配置、原始对话、桌面截图或含个人信息的文档。
- 拉取前检查工作区；有未提交修改时不要自动 stash、reset、clean 或覆盖。
- 只允许 fast-forward 拉取。遇到分叉或冲突立即停止并说明，不私自解决可能影响另一台电脑的冲突。
- 用户明确要求同步、保存到 GitHub 或推送时，才执行远程写入。
- 每次推送前更新 `context/PROJECT_STATE.md` 和 `context/TASK_LOG.md`，使另一台电脑无需依赖本机 Codex 数据库即可续接。
