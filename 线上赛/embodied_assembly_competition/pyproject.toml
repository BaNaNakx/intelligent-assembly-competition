[project]
name = "embodied-assembly-competition"
version = "0.1.0"
description = "Core modules for the embodied intelligent precision assembly competition."
requires-python = ">=3.11"
dependencies = ["pillow>=10.0"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
